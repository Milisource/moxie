# Build Assets

This directory contains platform-specific build assets for the Wails desktop application.

- `appicon.png` — Application icon (512×512 PNG)
- `darwin/` — macOS build configuration
- `windows/` — Windows build configuration (icons, manifests, installer)

Replace `appicon.png` with a proper application icon before release.
